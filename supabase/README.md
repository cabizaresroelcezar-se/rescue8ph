# Rescue 8 Philippines — Supabase Configuration
# This is a placeholder. Run `supabase init` after installing the Supabase CLI
# to generate the full config.toml. The migrations directory below is the
# source of truth for the database schema.